<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mic_partner.list">
      <field name="name">mic_partner list</field>
      <field name="model">mic_partner.mic_partner</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mic_partner.action_window">
      <field name="name">mic_partner window</field>
      <field name="res_model">mic_partner.mic_partner</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mic_partner.action_server">
      <field name="name">mic_partner server</field>
      <field name="model_id" ref="model_mic_partner_mic_partner"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mic_partner" id="mic_partner.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mic_partner.menu_1" parent="mic_partner.menu_root"/>
    <menuitem name="Menu 2" id="mic_partner.menu_2" parent="mic_partner.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mic_partner.menu_1_list" parent="mic_partner.menu_1"
              action="mic_partner.action_window"/>
    <menuitem name="Server to list" id="mic_partner" parent="mic_partner.menu_2"
              action="mic_partner.action_server"/>
    -->
  </data>
</odoo>