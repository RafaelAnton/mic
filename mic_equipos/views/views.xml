<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mic_equipos.list">
      <field name="name">mic_equipos list</field>
      <field name="model">mic_equipos.mic_equipos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mic_equipos.action_window">
      <field name="name">mic_equipos window</field>
      <field name="res_model">mic_equipos.mic_equipos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mic_equipos.action_server">
      <field name="name">mic_equipos server</field>
      <field name="model_id" ref="model_mic_equipos_mic_equipos"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mic_equipos" id="mic_equipos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mic_equipos.menu_1" parent="mic_equipos.menu_root"/>
    <menuitem name="Menu 2" id="mic_equipos.menu_2" parent="mic_equipos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mic_equipos.menu_1_list" parent="mic_equipos.menu_1"
              action="mic_equipos.action_window"/>
    <menuitem name="Server to list" id="mic_equipos" parent="mic_equipos.menu_2"
              action="mic_equipos.action_server"/>
    -->
  </data>
</odoo>